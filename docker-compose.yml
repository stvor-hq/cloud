version: "3.9"

services:
  relay:
    build: .
    ports:
      - "${RELAY_PORT:-8787}:8787"
    environment:
      PORT: "${RELAY_PORT:-8787}"
      STVOR_APP_TOKEN: "${RELAY_TOKEN:-stvor-relay-dev-token}"
